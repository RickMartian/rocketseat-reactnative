import { StatusBar } from 'react-native';

StatusBar.setBackgroundColor('#DA552F'); //cor de fundo da status bar no android
StatusBar.setBarStyle('light-content')//para ios, mexer no estilo da barra de status